# Streak is weekly and current-week only

Streaks are counted by consecutive ISO weeks (Monday-Sunday) in the player's local timezone, not by consecutive days. A session extends streaks only when its stored date falls in the current ISO week; sessions dated in prior weeks are treated as backfill and do not repair a broken streak, including when session dates are edited.
